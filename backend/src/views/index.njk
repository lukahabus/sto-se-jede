<!DOCTYPE html>
<html lang="hr">
  <head>
    <meta charset="UTF-8" />
    <meta http-equiv="X-UA-Compatible" content="IE=edge" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Šta se jede?</title>

    <link
      href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css"
      rel="stylesheet"
      integrity="sha384-1BmE4kWBq78iYhFldvKuhfTAU6auU8tT94WrHftjDbrCEXSU1oBoqyl2QvZ6jIW3"
      crossorigin="anonymous"
    />
    <link rel="stylesheet" href="/css/index.css" />
  </head>
  <body>
    {% include "_partials/navigationBar.njk" %}

    <main class="container-xxl">
      <div class="d-flex my-5">
        <h2 class="text-nowrap mt-1 mb-0 me-3">Odaberi svoju menzu</h2>
        {% include "_partials/menuSelect.njk" %}
      </div>

      <hr />

      {% for city in menu %}
        <h2>{{ city.name }}</h2>

        {% for canteen in city.canteens %}
          <h3>{{ canteen.name }}</h3>

          {% for meal in canteen.menu %}
            <h4>{{ meal.name }}</h4>

            <table class="table">
              <thead>
                <tr>
                  <th scope="col">Jelo</th>
                  <th scope="col">Energija</th>
                  <th scope="col">Ugljikohidrati</th>
                  <th scope="col">Proteini</th>
                  <th scope="col">Masti</th>
                  <th></th>
                </tr>
              </thead>
              <tbody>
                {% for dish in meal.dishes %}
                <tr>
                  <td>{{ dish.name }}</td>
                  <td>{{ dish.energy }} kcal</td>
                  <td>{{ dish.carbohydrates }} g</td>
                  <td>{{ dish.proteins }} g</td>
                  <td>{{ dish.fat }} g</td>
                  <td class="text-end">
                    <button type="button" class="btn btn-secondary btn-sm">
                      Označi kao pojedeno
                    </button>
                  </td>
                </tr>
                {% endfor %}
              </tbody>
            </table>
          {% else %}

          prazan raspored

          {% endfor %}
        {% else %}

        nema menzi

        {% endfor %}
      {% endfor %}

    </main>

    <script
      src="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/js/bootstrap.bundle.min.js"
      integrity="sha384-ka7Sk0Gln4gmtz2MlQnikT1wXgYsOg+OMhuP+IlRH9sENBO0LRn5q+8nbTov4+1p"
      crossorigin="anonymous"
    ></script>
    <script
      src="https://cdn.jsdelivr.net/npm/@hotwired/turbo@7.1.0/dist/turbo.es2017-umd.js"
      integrity="sha256-BbhQUDGjnGc5zodk3NDsZq8RgEj6m+eJ/8jftmQywSM="
      crossorigin="anonymous"
    ></script>
    <script src="/js/index.js"></script>
  </body>
</html>
